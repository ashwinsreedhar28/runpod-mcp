---
"@runpod/mcp-server": patch
---

Preserve literal API values and operation parameter overrides during tool generation. Keep recovery guidance on non-object API errors, and reject invalid JSON shapes at the journal storage boundary.
